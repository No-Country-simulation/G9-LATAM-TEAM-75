import { Routes } from '@angular/router';

/**
 * Tabla de rutas de la aplicación. Está vacía a propósito: EnergiAI es
 * una app de una sola pantalla (el dashboard completo vive en `App`,
 * `app.ts`/`app.html`), así que no hace falta navegar entre páginas.
 *
 * `provideRouter(routes)` en `app.config.ts` sigue registrando el motor
 * de enrutamiento de Angular por si en el futuro se agregan más vistas
 * (por ejemplo una página de detalle de un análisis, o de configuración).
 */
export const routes: Routes = [];
